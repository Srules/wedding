%p#notice= notice
%h1 Songs
%table
  %thead
    %tr
      %th Title
      %th Artist
      %th Url
      %th Suggested by
      %th Votes
      %th{:colspan => "3"}
  %tbody
    - @songs.each do |song|
      %tr
        %td= song.title
        %td= song.artist
        %td= song.url
        %td= song.suggested_by
        %td= song.votes
        %td= link_to 'Upvote', upvote_path(id: song.id), method: :post
        %td= link_to 'Downvote', upvote_path(id: song.id), method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Song', new_song_path
